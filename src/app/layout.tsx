import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NEXUS Platform - Next-Generation IT Solutions',
  description:
    'Transform your digital future with cutting-edge web applications. We craft innovative solutions using modern technology stacks.',
  keywords:
    'web development, React, Next.js, Supabase, IT solutions, digital transformation',
  authors: [{ name: 'NEXUS Platform Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={`${inter.className} antialiased`}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
